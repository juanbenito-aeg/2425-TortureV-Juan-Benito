export default class Element {
    constructor(id, mapRowIndex, mapColIndex) {
        this.id             = id;
        this.mapRowIndex    = mapRowIndex;
        this.mapColIndex    = mapColIndex;
    }
}